// It is not really magic numbers but rather the possibilities
// eslint-disable-next-line @typescript-eslint/no-magic-numbers
export const RADIUS_SIZES: number[] = [0, 3, 9, 15];

export const DEFAULT_RADIUS = 3;
